{
  "name": "faide",
  "version": "1.0.0",
  "private": true,
  "description": "FAIDE static storefront",
  "scripts": {
    "dev": "npx serve public",
    "start": "npx serve public",
    "sync": "bash ../scripts/sync-src-to-public.sh",
    "test:smoke": "node ../src/tests/smoke.mjs"
  }
}
